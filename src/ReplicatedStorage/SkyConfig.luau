-- Optional custom skybox. The game ships with Roblox's built-in sky at night (stars and a large
-- moon, set in default.project.json), tinted per zone by ZoneAmbience. To use your own skybox,
-- upload six images and paste their asset ids ("rbxassetid://123...") below; it is applied only
-- when all six are set.
return {
	SkyboxBk = "",
	SkyboxDn = "",
	SkyboxFt = "",
	SkyboxLf = "",
	SkyboxRt = "",
	SkyboxUp = "",
}
